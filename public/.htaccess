<IfModule mod_rewrite.c>
    Options -Multiviews
    RewriteEngine On

    RewriteCond %{REQUEST_URI} ^/(images|build|bundles|js|css)/
    RewriteRule ^ - [L]

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [QSA,L]
</IfModule>
