{% extends 'admin2/layouts/base.html.twig' %}
{% import 'admin2/orders/_macros.html.twig' as order_macros %}

{% block title %}Гарантія #{{ warranty.id }} — X-media Admin{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/admin2/product-edit.css') }}">
    <link rel="stylesheet" href="{{ asset('css/admin2/orders.css') }}">
{% endblock %}

{% block body %}
    <div class="product-edit-header">
        <div>
            <a href="{{ path('admin2_warranties') }}" class="text-decoration-none text-muted small d-inline-flex align-items-center gap-1 mb-2">
                <i class="bi bi-arrow-left"></i> До списку гарантій
            </a>
            <h1 class="page-title mb-1">Гарантія #{{ warranty.id }}</h1>
            <div class="product-edit-meta">{{ order_macros.warranty_status_badge(warranty.status) }} · {{ warranty.createdAt|date('d.m.Y H:i') }}</div>
        </div>
    </div>

    {% if form.vars.errors|length > 0 %}
        <div class="alert alert-danger">{{ form_errors(form) }}</div>
    {% endif %}

    {{ form_start(form, { attr: { class: 'product-edit-form' } }) }}

    <div class="card product-edit-section shadow-sm">
        <div class="card-header">Дані гарантії</div>
        <div class="card-body">
            <div class="row g-3">
                <div class="col-md-4">{{ form_row(form.status) }}</div>
                <div class="col-md-4">{{ form_row(form.name) }}</div>
                <div class="col-md-4">{{ form_row(form.surname) }}</div>
                <div class="col-md-4">{{ form_row(form.phone) }}</div>
                <div class="col-md-4">{{ form_row(form.email) }}</div>
                <div class="col-md-4">{{ form_row(form.orderNumber) }}</div>
                <div class="col-md-6">{{ form_row(form.fromClientTtn) }}</div>
                <div class="col-md-6">{{ form_row(form.toClientTtn) }}</div>
                <div class="col-md-6">{{ form_row(form.supplier) }}</div>
                <div class="col-md-6">{{ form_row(form.supplierOrderNumber) }}</div>
                <div class="col-md-6">{{ form_row(form.product) }}</div>
                <div class="col-md-6">{{ form_row(form.expenses) }}</div>
                <div class="col-12">{{ form_row(form.reason) }}</div>
            </div>
        </div>
    </div>

    <div class="product-edit-actions">
        <div class="card border-0 px-3 py-2 product-edit-actions__bar">
            <button type="submit" class="btn btn-teal"><i class="bi bi-check2"></i> Зберегти</button>
            <a href="{{ path('admin2_warranties') }}" class="btn btn-outline-secondary">Скасувати</a>
        </div>
    </div>

    {{ form_end(form) }}
{% endblock %}
