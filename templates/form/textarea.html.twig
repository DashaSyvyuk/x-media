{% set prefix = formPrefix|default('RozetkaProduct') %}
<textarea
        id="{{ prefix }}_values_{{ valueId }}_stringValue"
        name="{{ prefix }}[values][{{ valueId }}][stringValue]"
        class="rozetka-characteristics-values form-control"
>
</textarea>
