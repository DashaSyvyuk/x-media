{% set prefix = formPrefix|default('RozetkaProduct') %}
<select id="{{ prefix }}_values_{{ valueId }}_values" name="{{ prefix }}[values][{{ valueId }}][values][]" class="rozetka-characteristics-values form-select" multiple="multiple">
    {% for value in values %}
        <option value="{{ value.id }}">{{ value.value }}</option>
    {% endfor %}
</select>
