{% extends "base.html.twig" %}

{% block javascripts %}
    {{ encore_entry_script_tags('cart') }}
{% endblock %}

{% block body %}

    <header>
        {% include 'partials/header.html.twig' %}
    </header>

    <section>
        <div class="title">Про нас</div>

        <div class="breadcrumb">
            <a>X-media</a>
            <span> > </span>
            <a href="/">Головна</a>
            <span> > </span>
            <a class="selected">Контакти</a>
        </div>

        <div class="about-us">
            <h3>Наші контакти</h3>
            <h4>Інтернет-магазин x-media.com.ua</h4>
            <p>Телефон: {% for phone in phoneNumbers %}<span>{{ phone.getValue() }} </span>{% endfor %} </p>
            <p>Наш email: {% for email in emails %}<span>{{ email.getValue() }}</span> {% endfor %} </p>
        </div>
    </section>

{% endblock %}
