{% extends "base.html.twig" %}

{% block javascripts %}
    {{ encore_entry_script_tags('cart') }}
{% endblock %}

{% block body %}

    <header>
        {% include 'partials/header.html.twig' %}
    </header>

    <section>
        <div class="title">Дякуємо за покупку</div>

        <div class="breadcrumb">
            <a>X-media</a>
            <span> > </span>
            <a href="/">Головна</a>
            <span> > </span>
            <a class="selected">Дякуємо за покупку</a>
        </div>

        <div class="order-detail">
            <p class="thanks">Дякуємо за замовлення у нашому магазині, ми найближчим часом з вами зв'яжемося для уточнення деталей</p>
            <p><span>Деталі замовлення:</span></p>
            <table class="products">
                {% for item in order.items %}
                    <tr>
                        <td><a href="/products/{{ item.product.id }}"><img src="/{{ item.product.getImages()[0].imageUrl }}"></a></td>
                        <td><a href="/products/{{ item.product.id }}">{{ item.product.title }}</a></td>
                        <td>{{ item.count }}</td>
                    </tr>
                {% endfor %}
            </table>
            <p><span>Спосіб оплати: </span>{{ order.paytype }}</p>
            <p><span>Адреса: </span>{{ order.address }}</p>
            <p><span>Загальна вартість: </span>{{ order.total|number_format(0, '.', ' ') }} грн</p>
        </div>
    </section>

{% endblock %}
